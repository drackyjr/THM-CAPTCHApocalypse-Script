#!/usr/bin/env python3
from setuptools import setup, find_packages

setup(
    name="captcha-brute-force",
    version="3.2.0",
    description="Interactive CAPTCHA Brute Force Tool with Strength Selection",
    author="Your Name",
    python_requires=">=3.8",
    packages=find_packages(),
)
